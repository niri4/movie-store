<nav class="navbar navbar-default header-nav" role="navigation">
  <div class="navbar-header">

    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
       <span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span>
    </button> <a class="navbar-brand" href="#">Movie Store</a>
  </div>

  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav">
      <li class="active">
        <a href="<%= root_path%>">Home</a>
      </li>
      <li>

      </li>

    </ul>
    <div class="col-sm-8">
    <%= render 'shared/search'%>
  </div>
    <ul class="nav navbar-nav navbar-right">
      <li>
        <%if user_signed_in? %>

        <% else%>
        <li><a href="<%= new_user_registration_path%>"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
        <%end%>
      </li>
      <li class="dropdown">
        <% if user_signed_in? %>
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-user"></span><%= current_user.name.titleize%><strong class="caret"></strong></a>
        <%else%>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Action<strong class="caret"></strong></a>
          <%end%>
        <ul class="dropdown-menu">
          <% if user_signed_in? %>
          <li>
            <%= link_to "User Profile",edit_user_registration_path%>
          </li>
            <li>
              <%= link_to "SignOut",destroy_user_session_path,method: :delete%>
            </li>
          <% else %>
            <li>
              <li><a href="<%= new_user_registration_path%>"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
            </li>
            <li>
              <li><a href="<%= new_user_session_path%>"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
            </li>
          <% end%>

        </ul>
      </li>
    </ul>
  </div>

</nav>
